services:
  miniflux-digest:
    build: .
    container_name: miniflux-digest
    user: "1000:1000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./web/archive:/app/web/archive
    ports:
      - "3000:8080"
